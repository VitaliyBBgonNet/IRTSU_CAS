spring:
  datasource:
    url: ${SPRING_DATASOURCE_URL:jdbc:postgresql://prod-db-server:5432/cas}
    username: ${SPRING_DATASOURCE_USERNAME:bbgon}
    password: ${SPRING_DATASOURCE_PASSWORD:'!@QWASZX'}
    driver-class-name: org.postgresql.Driver

  jpa:
    hibernate:
      ddl-auto: update

  flyway:
    enabled: true
    locations: classpath:db/migration
    baseline-on-migrate: true
    validate-on-migrate: true
    schemas: public
    table: flyway_schema_history

server:
  port: 8080

jwt:
  secret: ${JWT_SECRET:a4c8e6f9b1a5c7d8f9a2b4e6d8f1b2c4d6e9f2a3c4e5f7a8d1c2e3f4a5b6d7e8f9a1b2c3d4e5f6a7b8c9d0e1f2}
  expiration: 86400000

logging:
  level:
    org.springframework.security: INFO

aes:
  secret-key: ${AES_SECRET_KEY:HfHkxhWHZY1ggSNXri60rw==}
